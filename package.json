{
  "name": "hebrew-tty",
  "version": "0.1.0",
  "description": "Hebrew that reads right in terminal coding agents, plus Hebrew dictation",
  "bin": {
    "hebrew-tty": "bin/hebrew-tty",
    "hebrew-voice": "bin/hebrew-voice"
  },
  "files": [
    "bin",
    "dist",
    "src/voice",
    "plugins",
    "herdr-plugin.toml",
    "tools/patch-binary.py",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build:tty": "cargo build --release",
    "prepack": "npm run build:tty && mkdir -p dist && cp target/release/hebrew-tty dist/hebrew-tty-linux-x86_64 && chmod 755 dist/hebrew-tty-linux-x86_64",
    "test": "node test/run.js && node test/voice.js && node test/binary.js"
  },
  "dependencies": {
    "bidi-js": "^1.0.3",
    "ws": "^8.21.3"
  },
  "keywords": [
    "hebrew",
    "rtl",
    "bidi",
    "terminal",
    "pty",
    "claude-code",
    "codex",
    "pi",
    "dictation"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "os": [
    "linux"
  ],
  "cpu": [
    "x64"
  ]
}
